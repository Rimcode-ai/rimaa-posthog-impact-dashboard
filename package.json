{
  "name": "posthog-impact-dashboard",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "serve": "python3 -m http.server 8765 --directory docs"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
